services:
  - type: web
    name: car-budget-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python car_budget_app/main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
    workingDir: .

